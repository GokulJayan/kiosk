<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart</title>
</head>

<body>
    <h2 class="text-center">Cart</h2>

    <div class="col-lg-11 col-md-8 m-auto">

        <table class="table mt-5 table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Name</th>
                    <th scope="col">Category</th>
                    <th scope="col">Description</th>
                    <th scope="col">Image</th>
                    <th scope="col">Price</th>
                    <th scope="col"> </th>
                    <th scope="col"> </th>
                </tr>
            </thead>

            <tbody>
                {{#each Items}}
                <tr>
                    <th scope="row">{{this.ID}}</th>
                     <td>{{this.Name}}</td>
                    <td>{{this.Category}}</td>
                    <td>{{this.Description}}</td>
                    <td><img href="#" style="width: 50px" src="http://localhost:3000/images/{{this.ID}}.jpeg" alt=""></td>
                    <td>₹{{this.Price}}</td>
                    <td>
                        <a href="/products/decrement/{{this.ID}}" class="fas fa-minus-circle btn m-auto text-primary"></a>
                        {{this.Count}}
                        <a href="/products/increment/{{this.ID}}" class="fas fa-plus-circle btn m-auto text-primary"></a>                   
                    </td>
                    <td>
                        <a href="/products/delete/{{this.ID}}" class="fas fa-trash-alt btn m-auto text-danger"
                            onclick="return confirm('Do you want to delete {{this.Name}} ?')"></a>
                    </td>
                </tr>
                {{/each}}

            </tbody>

        </table>
        <br>

        <div class="container">
            <div class="row mb-4 float-right">
                <a href="#" class="btn btn-primary m-auto">Total: ₹ {{total}}</a>
            </div>
        </div>
        <br><br><br>

        <div class="container">
            <div class="row mb-4">
                <a href="/checkout" class="btn btn-success m-auto">Check Out</a>
            </div>
        </div>
    </div>

    <script src="https://kit.fontawesome.com/949212ea46.js" crossorigin="anonymous"></script>



</body>

</html>